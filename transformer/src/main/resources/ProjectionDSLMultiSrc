Rule {
  from input: EntryStationRecord, ExitStationRecord
  to output: EntryTrafficTransaction {
   	PASSID <- EntryStationRecord.PASSID,
   	SITEID <- EntryStationRecord.ENTOLLSTATIONID,
   	EXITWEIGHT <- ExitStationRecord.EXITWEIGHT
  }
}